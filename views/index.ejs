<% include partials/header %>

<h1>Index Now</h1>

<% blogs.forEach(blog => { %>
 <div>
    <h2><%= blog.title %></h2>
    <img src=<%= blog.image%> alt="blog">
    <span><%= blog.created %></span>
     
 </div>

<% }) %>

<% include partials/footer %>